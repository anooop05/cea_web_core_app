<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/head') %>
    <title>Document</title>
  </head>
  <body>
    <%- include('./partials/nav') %>

    <div class="container mt-4">
      <h1><%= blog.title %></h1>
      <img src="<%= blog.coverImageURL %>" width="200px" />
      <pre class="mt-3"><%= blog.body %></pre>
    </div>
    <div class="container mt-4">
      <img
        src="<%= blog.createdBy.profileImageURL || '/images/default-avatar.png' %>"
        width="100px"
      />
      <%= blog.createdBy.fullName %>
    </div>

    <div class="container mt-3">
      <h1>Comments (<%= (comments.length) %>)</h1>
      <% if (locals.user) { %>
        <form action="/blog/comment/<%= blog._id %>" method="post">
          <div class="mb-3">
            <input
              type="text"
              class="form-control"
              name="content"
              placeholder="Enter your comment"
            />
            <button class="btn btn-sm btn-primary mt-2">Submit</button>
          </div>
        </form>
      </div>
      <% } %>
      <div class="mt-3">
            <% comments.forEach(comment => { %>
                <img
                src="<%= comment.createdBy.profileImageURL || '/images/default-avatar.png' %>"
                width="100px"
                /><%= comment.createdBy.fullName %>: <pre><%= comment.content %></pre>
            <% }) %>
        </div>

    <%- include('./partials/scripts') %>
  </body>
</html>
